%h2 Cerca luogo:
= form_tag '#' do
	= text_field '#', placeholder: "Cerca luogo", required: true
	= submit_tag "Cerca", name: nil

%hr/

%h3 Report più recenti:
= link_to 'Vedi i report più recenti',  reports_path

%br/ 
%br/ 
%hr/

-#%h3 Foto più votate:
-#= link_to 'Vedi le foto più votate', '#'

%h3 Popular pics:
- @photo_ids = PhotoLike.group("photo_id").order("COUNT(photo_id) DESC").take(3)
- @photo_ids.each do |photo_id|
-	id = photo_id.photo_id
-	photo = Photo.find(id)
	= image_tag photo.image.thumb.url
	%br/
	= link_to "View", photo_path(photo_id)
	%br

